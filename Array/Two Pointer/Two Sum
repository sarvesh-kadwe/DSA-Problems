// Rule -- If the array is unsorted, I use the HashMap approach to preserve the original indices. But if the array is already sorted, I use the two-pointer approach for better efficiency.”


class Solution {
    public int[] twoSum(int[] nums, int target) {
        Map<Integer,Integer> hmap = new HashMap<>();

        for(int i = 0;i<nums.length;i++)
        {
            int num = nums[i];
            int ans = target - num;

            if(hmap.containsKey(ans))
            {
                return new int[]{hmap.get(ans),i};
            }
            else {
                hmap.put(num,i);
            }
        }

        return new int[]{};
    }
}

